[package]
name = "lib-api"
version = "0.1.0"
edition = "2021"

[dependencies]

# -- axum
axum = { version = "0.8.1", features = ["multipart"] }

# -- serde json
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.139"

