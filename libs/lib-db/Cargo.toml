[package]
name = "lib-db"
version = "0.1.0"
edition = "2021"

# [profile.release]
# panic = "abort"

[dependencies]
# -- sql
sqlx = { version = "0.8", features = ["postgres", "runtime-tokio-rustls"] }

# -- error
thiserror = "2.0.11"
